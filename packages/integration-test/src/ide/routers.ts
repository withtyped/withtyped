import { createModelRouter, createQueryClient } from '@withtyped/postgres';
import { createRouter } from '@withtyped/server';

import {
  Book1,
  Book2,
  Book3,
  Book4,
  Book5,
  Book6,
  Book7,
  Book8,
  Book9,
  Book10,
  Book11,
  Book12,
  Book13,
  Book14,
  Book15,
  Book16,
  Book17,
  Book18,
  Book19,
  Book20,
  Book21,
  Book22,
  Book23,
  Book24,
  Book25,
  Book26,
  Book27,
  Book28,
  Book29,
  Book30,
  Book31,
  Book32,
  Book33,
  Book34,
  Book35,
  Book36,
  Book37,
  Book38,
  Book39,
  Book40,
  Book41,
  Book42,
  Book43,
  Book44,
  Book45,
  Book46,
  Book47,
  Book48,
  Book49,
  Book50,
} from './models.js';

const queryClient = createQueryClient();

const router1 = createModelRouter(Book1, queryClient).withCrud();

const router2 = createModelRouter(Book2, queryClient).withCrud();

const router3 = createModelRouter(Book3, queryClient).withCrud();

const router4 = createModelRouter(Book4, queryClient).withCrud();

const router5 = createModelRouter(Book5, queryClient).withCrud();

const router6 = createModelRouter(Book6, queryClient).withCrud();

const router7 = createModelRouter(Book7, queryClient).withCrud();

const router8 = createModelRouter(Book8, queryClient).withCrud();

const router9 = createModelRouter(Book9, queryClient).withCrud();

const router10 = createModelRouter(Book10, queryClient).withCrud();

const router11 = createModelRouter(Book11, queryClient).withCrud();

const router12 = createModelRouter(Book12, queryClient).withCrud();

const router13 = createModelRouter(Book13, queryClient).withCrud();

const router14 = createModelRouter(Book14, queryClient).withCrud();

const router15 = createModelRouter(Book15, queryClient).withCrud();

const router16 = createModelRouter(Book16, queryClient).withCrud();

const router17 = createModelRouter(Book17, queryClient).withCrud();

const router18 = createModelRouter(Book18, queryClient).withCrud();

const router19 = createModelRouter(Book19, queryClient).withCrud();

const router20 = createModelRouter(Book20, queryClient).withCrud();

const router21 = createModelRouter(Book21, queryClient).withCrud();

const router22 = createModelRouter(Book22, queryClient).withCrud();

const router23 = createModelRouter(Book23, queryClient).withCrud();

const router24 = createModelRouter(Book24, queryClient).withCrud();

const router25 = createModelRouter(Book25, queryClient).withCrud();

const router26 = createModelRouter(Book26, queryClient).withCrud();

const router27 = createModelRouter(Book27, queryClient).withCrud();

const router28 = createModelRouter(Book28, queryClient).withCrud();

const router29 = createModelRouter(Book29, queryClient).withCrud();

const router30 = createModelRouter(Book30, queryClient).withCrud();

const router31 = createModelRouter(Book31, queryClient).withCrud();

const router32 = createModelRouter(Book32, queryClient).withCrud();

const router33 = createModelRouter(Book33, queryClient).withCrud();

const router34 = createModelRouter(Book34, queryClient).withCrud();

const router35 = createModelRouter(Book35, queryClient).withCrud();

const router36 = createModelRouter(Book36, queryClient).withCrud();

const router37 = createModelRouter(Book37, queryClient).withCrud();

const router38 = createModelRouter(Book38, queryClient).withCrud();

const router39 = createModelRouter(Book39, queryClient).withCrud();

const router40 = createModelRouter(Book40, queryClient).withCrud();

const router41 = createModelRouter(Book41, queryClient).withCrud();

const router42 = createModelRouter(Book42, queryClient).withCrud();

const router43 = createModelRouter(Book43, queryClient).withCrud();

const router44 = createModelRouter(Book44, queryClient).withCrud();

const router45 = createModelRouter(Book45, queryClient).withCrud();

const router46 = createModelRouter(Book46, queryClient).withCrud();

const router47 = createModelRouter(Book47, queryClient).withCrud();

const router48 = createModelRouter(Book48, queryClient).withCrud();

const router49 = createModelRouter(Book49, queryClient).withCrud();

const router50 = createModelRouter(Book50, queryClient).withCrud();

export const router = createRouter()
  .pack(router1)
  .pack(router2)
  .pack(router3)
  .pack(router4)
  .pack(router5)
  .pack(router6)
  .pack(router7)
  .pack(router8)
  .pack(router9)
  .pack(router10)
  .pack(router11)
  .pack(router12)
  .pack(router13);
// .pack(router14)
// .pack(router15)
// .pack(router16)
// .pack(router17)
// .pack(router18)
// .pack(router19)
// .pack(router20)
// .pack(router21)
// .pack(router22)
// .pack(router23)
// .pack(router24)
// .pack(router25)
// .pack(router26)
// .pack(router27)
// .pack(router28)
// .pack(router29)
// .pack(router30)
// .pack(router31)
// .pack(router32)
// .pack(router33)
// .pack(router34)
// .pack(router35)
// .pack(router36)
// .pack(router37)
// .pack(router38)
// .pack(router39)
// .pack(router40)
// .pack(router41)
// .pack(router42)
// .pack(router43)
// .pack(router44)
// .pack(router45)
// .pack(router46)
// .pack(router47)
// .pack(router48)
// .pack(router49)
// .pack(router50);
