{
  "name": "@withtyped/shared",
  "version": "0.1.0",
  "author": "Gao Sun",
  "license": "MIT",
  "type": "module",
  "source": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./lib/index.js"
    },
    "./*": {
      "import": "./*"
    }
  },
  "module": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "prepack": "pnpm build",
    "build": "rm -rf lib/ && tsc",
    "dev": "pnpm build --watch --preserveWatchOutput --incremental",
    "dev:test": "pnpm build && pnpm test",
    "test": "node --test | tap-arc",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@silverhand/eslint-config": "^1.3.0",
    "@silverhand/ts-config": "^1.2.1",
    "@types/node": "^18.11.9",
    "eslint": "^8.28.0",
    "lint-staged": "^13.0.4",
    "prettier": "^2.7.1",
    "tap-arc": "^0.3.5",
    "typescript": "^4.9.3"
  },
  "eslintConfig": {
    "extends": "@silverhand"
  },
  "prettier": "@silverhand/eslint-config/.prettierrc",
  "publishConfig": {
    "access": "public"
  }
}
